const Loading = () => {
	return (
		<div className='fixed inset-0 grid place-items-center bg-n-1 z-30'>
			<img src='/loading.gif' alt='Loading' width={160} />
		</div>
	);
};

export default Loading;
